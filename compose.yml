services:
  nats:
    image: nats:alpine

  client:
    build:
      context: .
    develop:
      watch:
        - path: composer.json
          action: rebuild
        - path: composer.lock
          action: rebuild
        - path: .
          target: /app
          action: sync+restart
          ignore:
            - vendor/
            - compose.yml
            - Dockerfile
            - Makefile
