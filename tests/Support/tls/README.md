# Test TLS Certificates

Certificates in this directory are generated only for local and CI integration tests.
They are not intended for production use.

`cluster/` contains a dedicated CA and node/client certificates for TLS cluster failover tests.
